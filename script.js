/*const fizzBuzz = (entrada) => {
    if(typeof entrada != 'number'){
        return 'Not a Number'
    } else if (entrada % 5 === 0 && entrada % 3 === 0){
        return 'FizzBuzz'
    } else if (entrada % 3 === 0){
        return 'Fizz'
    } else if (entrada % 5 === 0){
        return 'Buzz'
    } else {
        return entrada
    }
}

const resultado = fizzBuzz(30);
console.log(resultado) */

//######################### Map ##########################
/*const numbers = [1,2,3,4]

const squareNumbers = numbers.map(arr => arr ** 2) 

console.log(squareNumbers); */

//######################### Reduce ##########################

console.log('Olá Diego')